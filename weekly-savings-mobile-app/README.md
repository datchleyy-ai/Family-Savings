# Weekly Savings Tracker

A no-login mobile-friendly savings tracker for two people.

Run it:

```powershell
npm start
```

Then open:

```text
http://localhost:4173
```

The app saves changes to `data/tracker.json`. To use it on both phones at home, run it on one computer and open the computer's local network address from each phone.
